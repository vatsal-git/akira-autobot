# LiteLLM Router configuration
# When use_dynamic_free_models is true, free models are fetched from OpenRouter API at startup.
# Requires OPENROUTER_API_KEY environment variable.

settings:
  use_dynamic_free_models: true

# --- COMMENTED OUT: Static model definitions (used when use_dynamic_free_models is false) ---
# model_list:
#   - model_name: openrouter-gemini-flash-lite
#     litellm_params:
#       model: openrouter/google/gemini-2.5-flash-lite
#       api_key: ${OPENROUTER_API_KEY}
#
#   - model_name: anthropic
#     litellm_params:
#       model: bedrock/${CLAUDE_INFERENCE_PROFILE}
#       aws_region_name: ${AWS_REGION}
