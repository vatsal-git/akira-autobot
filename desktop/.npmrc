# Allow npm and Electron's post-install download behind corporate proxies
# that use a self-signed certificate (e.g. SSL inspection).
# Remove this file if you're not behind such a proxy.
strict-ssl=false
